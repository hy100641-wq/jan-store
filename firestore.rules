rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Global open read/write access during development
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
